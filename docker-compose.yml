version: '3.8'

services:
  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_DB=digitalbook
      - POSTGRES_USER=digitalbook
      - POSTGRES_PASSWORD=digitalbook
    ports:
      - "5432:5432"

  backend:
    build: ./digitalbook
    command: sh -c "python manage.py collectstatic --no-input && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./digitalbook:/app
      - static_volume:/app/staticfiles
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql://digitalbook:digitalbook@db:5432/digitalbook
      - DEBUG=1

  # frontend:
  #   build: ./frontend
  #   volumes:
  #     - ./frontend:/app
  #     - /app/node_modules
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #   depends_on:
  #     - backend

volumes:
  postgres_data:
  static_volume:
